import SiteName from "./SiteName";
import Menu from "./Menu";

function Header(){

    return(
        <div>
            <SiteName />
            <Menu />
        </div>
    )
}
export default Header;
